//each test could take set of arguments to compare with a callback name and calles that function and checks the output to be true and false and gives output to a table field next to the function runed.

// all Function names are calculated and all classes are calculated.


